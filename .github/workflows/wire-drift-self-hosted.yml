name: Wire drift (self-hosted)

# Runs `bun scripts/check-wire-drift.mjs` against the installed `claude` on
# the self-hosted runner. This is the RUNTIME counterpart to cc-drift-watch.yml
# (which statically scans the installed Claude Code package and so can't see
# per-request wire decisions): it spawns claude for opus/sonnet/haiku/fable,
# reading each anthropic-beta header + billing block, and fails if
#
#   - betaForModel no longer reproduces CC's per-model beta set (the 2.1.170 ->
#     2.1.199 drift that shipped silently: sonnet keeping mid-conversation-
#     system, haiku dropping afk-mode + reordering, fable/[1m] positions), or
#   - dario's cch gate disagrees with CC's billing block (CC emits a cch we
#     have no seed for, or we hold a seed for a version CC no longer stamps).
#
# Mechanism: spawn the installed `claude` against a loopback endpoint (stub
# key, no OAuth) and read the request it sends — no real Anthropic call, so
# this needs only `claude` on PATH, not a subscription. The `dario-drift`
# runner already has claude installed (it hosts the template drift watcher).
# GH-hosted runners have no claude, so they can't host this.
#
# Fork PRs are skipped — a self-hosted runner must never run fork code.

on:
  pull_request:
    paths:
      # The wire-shape surface this check guards.
      - 'src/proxy.ts'
      - 'src/cch.ts'
      - 'src/cc-template.ts'
      - 'src/cc-template-data.json'
      - 'scripts/check-wire-drift.mjs'
      - '.github/workflows/wire-drift-self-hosted.yml'
  schedule:
    # Daily at 07:07 UTC — catches CC-side changes (a new claude on the runner
    # emitting a different per-model beta set) without waiting for a PR.
    # The old 06:37 slot collided with doctor-watch's `37 */6` tick on the
    # same single-slot self-hosted runner; 07:07 shares no minute/hour with
    # the repo's other crons.
    - cron: '7 7 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: wire-drift-${{ github.workflow }}
  cancel-in-progress: false

jobs:
  wire-drift:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    # Fork-PR guard: only the source repo runs on the self-hosted runner.
      # Never from a fork. This job is meaningless anywhere but this
      # repository, and 54 forks inherit the schedule. See #1039.
    if: >-
      github.repository == 'askalf/dario'
      && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    runs-on: [self-hosted, dario-drift]
    timeout-minutes: 10
    steps:
      - uses: askalf/checkout-with-retry@115a6407547e9711edbc2e915838d495cad9583f  # v1.1.0

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: canary

      - name: Install + build
        run: |
          bun install --frozen-lockfile
          bun run build

      - name: Report claude version
        run: claude --version || echo "::warning::claude not on PATH — the wire-drift check will report a high-severity 'capture' finding"

      - name: Run wire-drift check
        id: drift
        run: |
          set +e
          bun scripts/check-wire-drift.mjs > wire-drift-report.json 2> wire-drift-log.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
          echo "=== wire-drift-log.txt ==="
          cat wire-drift-log.txt
          echo "=== wire-drift-report.json ==="
          cat wire-drift-report.json

      - name: Comment on PR with result
        if: github.event_name == 'pull_request' && always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          pr=${{ github.event.pull_request.number }}
          marker='<!-- dario-wire-drift -->'
          existing=$(gh api "repos/${{ github.repository }}/issues/$pr/comments" --jq ".[] | select(.body | startswith(\"$marker\")) | .id" | head -1)

          status_emoji="❌"; status_text="DRIFT"
          if [ "${{ steps.drift.outputs.exit_code }}" = "0" ]; then status_emoji="✅"; status_text="CLEAN"; fi

          body_file=$(mktemp)
          {
            echo "$marker"
            echo "## Wire drift: $status_emoji $status_text"
            echo ""
            echo "Ran \`bun scripts/check-wire-drift.mjs\` against the runner's \`claude\` for commit \`${{ github.event.pull_request.head.sha }}\` — per-model \`anthropic-beta\` (betaForModel) + billing-block cch gate vs the installed \`claude\`."
            echo ""
            echo "<details><summary>Report</summary>"
            echo ""
            echo '```json'
            cat wire-drift-report.json 2>/dev/null || echo "(no report captured)"
            echo '```'
            echo ""
            echo "</details>"
            echo ""
            echo "[Full workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
          } > "$body_file"

          if [ -n "$existing" ]; then
            gh api -X PATCH "repos/${{ github.repository }}/issues/comments/$existing" -f body="$(cat "$body_file")" > /dev/null
          else
            gh pr comment "$pr" --body-file "$body_file"
          fi

      - name: Open / update drift issue (scheduled runs)
        if: (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && steps.drift.outputs.exit_code != '0'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CC_VERSION=$(node -e "try{console.log(JSON.parse(require('fs').readFileSync('wire-drift-report.json','utf-8')).ccVersion||'unknown')}catch(e){console.log('unknown')}")
          TITLE="Wire drift detected: CC v${CC_VERSION}"
          EXISTING=$(gh issue list --label cc-drift --state open --search "in:title \"${TITLE}\"" --json number --jq '.[0].number')
          body_file=$(mktemp)
          {
            echo "\`scripts/check-wire-drift.mjs\` found a per-model beta or cch-gate divergence from the installed \`claude\` (CC v${CC_VERSION})."
            echo ""
            echo '```json'
            cat wire-drift-report.json 2>/dev/null || echo "(no report)"
            echo '```'
            echo ""
            echo "Fix: update \`betaForModel\` (src/proxy.ts) to the current CC matrix, and/or run \`bun run cch:calibrate\` if the report shows a cch-gate finding. [Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
          } > "$body_file"
          if [ -n "$EXISTING" ]; then
            gh issue comment "$EXISTING" --body-file "$body_file"
          else
            gh issue create --title "$TITLE" --body-file "$body_file" --label cc-drift
          fi

      - name: Set job status
        if: always()
        env:
          DRIFT_EXIT_CODE: ${{ steps.drift.outputs.exit_code }}
        run: exit "$DRIFT_EXIT_CODE"
